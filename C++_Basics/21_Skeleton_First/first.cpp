#include<iostream>
using namespace std; //whatevr inside that namespace i.e, std, we are using it
int main()          //all the built in things in the header file are grpd into one name : namespace
{
     cout<<"hello world"; //or we can write std::cout<<"hello world";
     // cout<<"C++ version is "<<__cplusplus<<"\n";
     return 0;
}