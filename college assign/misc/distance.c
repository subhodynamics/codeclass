#include<stdio.h>
#include<conio.h>
#include<math.h>

int main()
{
    int x1=2,x2=3,y1=3,y2=8,di;
    di=sqrt(pow(x2-x1,2)+pow(y2-y1,2));
    printf("distance between these two points is %d",di);
    return 0;
}

//! Subhadeep pathak 0176